<!-- ═══════════════════════ HEADER ═══════════════════════ -->

<img src="https://capsule-render.vercel.app/api?type=waving&color=0:0d1117,45:1f2937,100:F7A41D&height=200&section=header&text=Oualid%20Obbad&fontSize=50&fontColor=ffffff&fontAlignY=34&desc=Software%20Engineer%20%E2%80%A2%20Backend%20Development&descSize=18&descAlignY=54&animation=fadeIn" width="100%" alt="Oualid Obbad — Software Engineer, Backend Development" />

<p align="center">
  <img src="https://readme-typing-svg.demolab.com?font=JetBrains+Mono&weight=600&size=21&duration=2800&pause=900&color=F7A41D&center=true&vCenter=true&width=680&lines=Software+Engineer+%E2%80%A2+Backend+Development%3BC+%E2%80%A2+C%2B%2B98+%E2%80%A2+Java%3BHTTP+Servers%2C+Shells%2C+Concurrency%3BSpring+%E2%80%A2+PostgreSQL+%E2%80%A2+Docker%3B1337+%2F+42+Network+%28UM6P%29+%E2%80%A2+Rabat" alt="Typing SVG" />
</p>

<p align="center">
  <a href="mailto:oualidobbad@gmail.com"><img src="https://img.shields.io/badge/Email-D14836?style=for-the-badge&logo=gmail&logoColor=white" alt="Email" /></a>
  <a href="https://www.linkedin.com/in/oualid-obbad-0b4102302/"><img src="https://img.shields.io/badge/LinkedIn-0A66C2?style=for-the-badge&logo=linkedin&logoColor=white" alt="LinkedIn" /></a>
  <a href="https://github.com/obbad-dev"><img src="https://img.shields.io/badge/GitHub-181717?style=for-the-badge&logo=github&logoColor=white" alt="GitHub" /></a>
  <a href="https://profile.intra.42.fr/users/oobbad"><img src="https://img.shields.io/badge/42%20Intra-000000?style=for-the-badge&logo=42&logoColor=white" alt="42 Intra" /></a>
  <img src="https://img.shields.io/badge/Casablanca,%20Morocco-F7A41D?style=for-the-badge&logo=googlemaps&logoColor=white" alt="Casablanca, Morocco" />
</p>

---

## Contents

[Profile](#profile) · [Education](#education) · [Technical Skills](#tech-stack) · [Featured Projects](#featured-projects) · [Additional Projects](#additional-projects) · [Languages](#languages) · [GitHub Activity](#github-activity) · [Currently](#currently)

---

<!-- ═══════════════════════ PROFILE ═══════════════════════ -->

## Profile

Software engineering student at **1337 / 42 Network (UM6P)**, focused on **backend development with C, C++ and Java**. I build real systems from scratch — web servers, a Unix shell, containerized applications — with a solid foundation in **networking, databases and Linux**. Currently seeking a backend engineering internship.

<table width="100%">
<tr>
<td width="14%">

$\textcolor{#F7A41D}{\textsf{\textbf{Role}}}$

</td>
<td width="41%">Software Engineer · Backend Development</td>
<td width="45%" align="center" valign="middle" rowspan="7">
  <a href="https://profile.intra.42.fr/users/oobbad">
    <img src="https://raw.githubusercontent.com/obbad-dev/obbad-dev/main/assets/42-badge.svg?v=9ba3bb17" alt="Oualid Obbad — live 42 level and grade" width="400" />
  </a>
  <br/><br/>
  <sub>Live from the 42 intranet. Level and grade advance only on projects validated in peer code review.</sub>
</td>
</tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{Based in}}}$

</td><td>Casablanca, Morocco</td></tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{School}}}$

</td><td>1337 Coding School (42 Network) — UM6P, Rabat</td></tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{Prior degree}}}$

</td><td>BTS — Computer Systems and Networks</td></tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{Core stack}}}$

</td><td>C · C++98 · Java · Spring · PostgreSQL · Docker · Linux</td></tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{Building now}}}$

</td><td><a href="#featured-projects">ft_transcendence</a> — real-time multi-user containerized platform</td></tr>
<tr><td>

$\textcolor{#F7A41D}{\textsf{\textbf{Open to}}}$

</td><td>Backend engineering internships</td></tr>
</table>

> **How I work:** understand it end to end, or you don't understand it. Every project below was implemented from a written specification rather than assembled from libraries — an HTTP/1.1 server on a non-blocking `epoll` loop, a Unix shell, a layered Java application over PostgreSQL, and a multi-container Docker stack.

---

<!-- ═══════════════════════ EDUCATION ═══════════════════════ -->

## Education

| Institution | Program | Period |
|---|---|---|
| **1337 Coding School (42 Network) — UM6P**, Rabat | Software Engineering | `2024 – Present` |
| **BTS** | Advanced Technician Certificate in Computer Systems and Networks | `2024` |
| **Baccalaureate** | Electrical Science and Technology | `2022` |

<sub><b>1337 / 42 Network</b> is a peer-to-peer, project-based curriculum with no lectures and no teachers. Every project is built from a written specification under strict technical constraints — restricted function sets, enforced coding standards, mandatory memory-leak checks — and defended in peer code review before it is graded.</sub>

---

<!-- ═══════════════════════ STACK ═══════════════════════ -->

## Tech Stack

<table width="100%">
  <tr>
    <td width="170"><b>Core Languages</b></td>
    <td><img src="https://skillicons.dev/icons?i=c,cpp,java&theme=dark" alt="C, C++, Java" height="42" /></td>
  </tr>
  <tr>
    <td><b>Backend</b></td>
    <td><img src="https://skillicons.dev/icons?i=spring,maven,postgres,mysql&theme=dark" alt="Spring, Maven, PostgreSQL, MySQL" height="42" /></td>
  </tr>
  <tr>
    <td><b>Systems &amp; DevOps</b></td>
    <td><img src="https://skillicons.dev/icons?i=linux,docker,nginx,bash,git,github,vim&theme=dark" alt="Linux, Docker, NGINX, Bash, Git, GitHub, Vim" height="42" /></td>
  </tr>
</table>

| Area | Technologies |
|---|---|
| **Languages** | C, C++ (C++98), Java, Bash |
| **Backend** | Spring, Spring JDBC, JDBC, Maven, HikariCP, HTTP/1.1, REST, CGI |
| **Databases** | SQL, MySQL, PostgreSQL, MariaDB, HSQLDB, schema design, normalization |
| **Systems & Networking** | Linux, POSIX threads, processes, signals, TCP/IP sockets, `epoll`, IP addressing, subnetting (CIDR), routing |
| **DevOps & Tools** | Docker, Docker Compose, NGINX, TLS/SSL, Git, Make, Valgrind |
| **Testing** | JUnit 5, Mockito, integration testing |

<sub><b>Also worked with:</b> JavaScript · HTML/CSS · Python — coursework and side projects.</sub>

---

<!-- ═══════════════════════ PROJECTS ═══════════════════════ -->

## Featured Projects

<table width="100%">
<tr>
<td width="50%" valign="top">

### [webserv](https://github.com/obbad-dev/webServ)
**HTTP/1.1 web server in C++98** · *team of 2*

An NGINX-inspired server written from scratch, no external libraries. Single-threaded non-blocking event loop on `epoll()`, `GET`/`POST`/`DELETE`, a hand-written tokenizer and parser for an NGINX-style config, virtual hosting, CGI execution, multipart uploads, autoindex, custom error pages and redirects.

<img src="https://img.shields.io/badge/C++98-00599C?style=flat-square&logo=cplusplus&logoColor=white" /> <img src="https://img.shields.io/badge/epoll-F7A41D?style=flat-square" /> <img src="https://img.shields.io/badge/Sockets-161b22?style=flat-square" /> <img src="https://img.shields.io/badge/CGI-161b22?style=flat-square" />

<sub>Network programming · I/O multiplexing · implementing a protocol from an RFC · parser design</sub>

</td>
<td width="50%" valign="top">

### [java_backend](https://github.com/obbad-dev/pool_java_part2)
**Java backend engineering**

Four Maven modules covering a real backend's layers: a chat app on a normalized PostgreSQL schema with a JDBC repository layer and HikariCP pooling, JUnit 5 + Mockito tests against an embedded DB, a **custom ORM** driven by reflection and annotations, and DI through the Spring container.

<img src="https://img.shields.io/badge/Java-ED8B00?style=flat-square&logo=openjdk&logoColor=white" /> <img src="https://img.shields.io/badge/Spring-6DB33F?style=flat-square&logo=spring&logoColor=white" /> <img src="https://img.shields.io/badge/PostgreSQL-4169E1?style=flat-square&logo=postgresql&logoColor=white" /> <img src="https://img.shields.io/badge/JUnit%205-25A162?style=flat-square&logo=junit5&logoColor=white" />

<sub>Layered architecture · relational modelling · dependency injection · testing discipline</sub>

</td>
</tr>
<tr>
<td width="50%" valign="top">

### [minishell](https://github.com/obbad-dev/minishell)
**Unix shell in C** · *team of 2*

A Bash-like shell built with no parsing libraries. Full pipeline — lexer → expansion → parser → execution — with pipes, all redirections, heredocs, quoting rules, `$VAR` and `$?` expansion, seven builtins, Bash-accurate signals, and a custom tracking allocator for leak-free cleanup.

<img src="https://img.shields.io/badge/C-00599C?style=flat-square&logo=c&logoColor=white" /> <img src="https://img.shields.io/badge/Processes-F7A41D?style=flat-square" /> <img src="https://img.shields.io/badge/Signals-161b22?style=flat-square" /> <img src="https://img.shields.io/badge/Readline-161b22?style=flat-square" />

<sub>Process &amp; file-descriptor management · tokenizer and parser design · memory discipline</sub>

</td>
<td width="50%" valign="top">

### [inception](https://github.com/obbad-dev/inception-42)
**Containerized infrastructure**

An 8-service stack where every service runs in its own container from a hand-written Dockerfile: NGINX reverse proxy with TLSv1.2/1.3 as the sole entrypoint, WordPress on PHP-FPM, MariaDB, Redis, Adminer, FTP and local DNS — on an isolated bridge network with Docker secrets and persistent volumes.

<img src="https://img.shields.io/badge/Docker-2496ED?style=flat-square&logo=docker&logoColor=white" /> <img src="https://img.shields.io/badge/NGINX-009639?style=flat-square&logo=nginx&logoColor=white" /> <img src="https://img.shields.io/badge/MariaDB-003545?style=flat-square&logo=mariadb&logoColor=white" /> <img src="https://img.shields.io/badge/TLS-F7A41D?style=flat-square" />

<sub>Container orchestration · service isolation · reverse proxying &amp; TLS · secret handling</sub>

</td>
</tr>
<tr>
<td width="50%" valign="top">

### [cub3D](https://github.com/obbad-dev/cub3d_mandatory)
**Raycasting 3D engine in C**

A Wolfenstein-style renderer turning a 2D tile map into a navigable 3D view: DDA raycasting with horizontal/vertical intersection selection, per-direction wall textures, a real-time loop at 1920×1080, a minimap, and a fully validated `.cub` scene parser.

<img src="https://img.shields.io/badge/C-00599C?style=flat-square&logo=c&logoColor=white" /> <img src="https://img.shields.io/badge/Raycasting-F7A41D?style=flat-square" /> <img src="https://img.shields.io/badge/MiniLibX-161b22?style=flat-square" />

<sub>Applied geometry · real-time rendering · modular C design</sub>

</td>
<td width="50%" valign="top">

### [philosophers](https://github.com/obbad-dev/philosophers_42)
**Concurrency in C**

The Dining Philosophers problem under strict timing constraints: deadlock avoidance via parity-ordered acquisition, race-free shared state, a monitoring thread detecting starvation at millisecond resolution, and clean shutdown of every thread.

<img src="https://img.shields.io/badge/C-00599C?style=flat-square&logo=c&logoColor=white" /> <img src="https://img.shields.io/badge/pthreads-F7A41D?style=flat-square" /> <img src="https://img.shields.io/badge/Mutexes-161b22?style=flat-square" />

<sub>Multithreading · synchronization primitives · deadlock &amp; race reasoning</sub>

</td>
</tr>
<tr>
<td colspan="2" valign="top">

### ft_transcendence &nbsp;<img src="https://img.shields.io/badge/In%20Progress-F7A41D?style=flat-square" />
**Full-stack real-time platform** · *team of 4*

The 42 curriculum's final project. Developing a **containerized multi-user web application** orchestrated with Docker for single-command deployment, serving **HTTPS REST APIs** and **real-time WebSocket pipelines**. Building backend services with **secure user authentication**, end-to-end input validation, and a relational database schema optimized for **multi-client concurrency**.

<img src="https://img.shields.io/badge/Docker-2496ED?style=flat-square&logo=docker&logoColor=white" /> <img src="https://img.shields.io/badge/WebSockets-161b22?style=flat-square" /> <img src="https://img.shields.io/badge/REST-161b22?style=flat-square" /> <img src="https://img.shields.io/badge/PostgreSQL-4169E1?style=flat-square&logo=postgresql&logoColor=white" /> <img src="https://img.shields.io/badge/HTTPS-6DB33F?style=flat-square" />

<sub>Real-time systems · API design · authentication &amp; validation · container orchestration</sub>

</td>
</tr>
</table>

<!-- ═══════════════════════ DEEP DIVES ═══════════════════════ -->

### Deep dives

<details>
<summary><b>webserv — request lifecycle, architecture and engineering challenges</b></summary>

<br>

**Highlights**

- **Non-blocking event loop.** One thread multiplexes every listening and client socket through `epoll()`. Partial reads and partial writes are carried across loop iterations via `EPOLLOUT`, so no single request can ever block the server.
- **Incremental request parsing.** Detects header termination (`\r\n\r\n`) and consumes bodies through both `Content-Length` and **chunked transfer encoding**, reading only what is available on each event.
- **Hand-written configuration parser.** A tokenizer and recursive parser for an NGINX-style config compile directives into a `Server` / `Location` object tree that drives routing, method validation and per-server `client_max_body_size` limits.
- **CGI execution.** `fork` + `pipe` with full RFC-compliant environment setup (`REQUEST_METHOD`, `QUERY_STRING`, `PATH_INFO`, `CONTENT_TYPE`, `CONTENT_LENGTH`), streaming the request body to the child's stdin and parsing CGI-generated headers back out.
- **Feature set.** Virtual hosting with `server_name` matching, directory autoindex, configurable per-server error pages, `301`/`302` redirects, `multipart/form-data` uploads with boundary parsing, MIME type resolution and URL decoding.

**Request lifecycle**

```
Startup      main() → ParseConfig → Server[] / Location[]
             OpenConnection::initServer()  socket → bind → listen → register in epoll

Event loop   epoll() on all fds
               ├── EPOLLIN on listener  → acceptNewConnection()  accept + register client fd
               └── EPOLLIN on client    → handleClientData()     incremental read

Routing      matchServer()      select virtual server by Host header + port
             HTTP::handleRequest()
               1. parseRequest()    method, URI, headers
               2. matchLocation()   longest-prefix route match
               3. validate          method allowed? body within limit?
               4. dispatch          GET → serve / autoindex
                                    POST → upload or executeCGI()
                                    DELETE → unlink
               5. buildResponse()   status line + headers + body

Write        sendResponse()     partial sends resumed across epoll cycles
Cleanup      closeConnection()  deregister fd → close()
```

**Class responsibilities**

| Class | Role |
|---|---|
| `ParseConfig` | Tokenizes the `.conf` file and builds the `Server`/`Location` tree |
| `Server` / `Location` | Hold per-server and per-route configuration |
| `OpenConnection` | Socket lifecycle and the `epoll` event loop |
| `HTTP` | Request parsing, routing, handler dispatch, response building |
| `HelperFunc` | MIME types, URL decoding, shared utilities |

**Engineering challenges**

- **No blocking calls anywhere.** Every `read`, `write` and `accept` had to tolerate returning partial data, which means per-client state machines rather than straight-line code.
- **C++98 only.** No `auto`, no range-based `for`, no smart pointers, no `std::to_string` — ownership and lifetimes are managed explicitly.
- **Protocol correctness from the RFC.** Chunked decoding, header folding, status-code semantics and CGI header merging were implemented from the specification rather than a reference library.
- **Reaping CGI children** without blocking the event loop, and cleaning up their pipes on client disconnect.

</details>

<details>
<summary><b>Java backend suite — module breakdown and schema</b></summary>

<br>

**Highlights**

- **Layered chat application** over a **normalized 4-table PostgreSQL schema** with foreign-key constraints and a composite-key junction table modelling many-to-many membership.
- **Repository / DAO layer.** `UsersRepository` and `MessagesRepository` are interfaces with JDBC implementations behind them, decoupling persistence from domain models and making the backing store substitutable.
- **Connection pooling** with HikariCP over the PostgreSQL JDBC driver, packaged into deployable fat JARs with the Maven Shade plugin.
- **Custom mini-ORM.** Runtime annotations (`@OrmEntity`, `@OrmColumn`, `@OrmColumnId`) plus Java reflection map POJOs to relational tables and generate `CREATE TABLE`, `INSERT`, `UPDATE` and `SELECT` statements dynamically — a from-scratch reimplementation of what Hibernate does.
- **Annotation processor** that generates HTML forms from annotated classes, demonstrating framework-level metaprogramming.
- **Testing pipeline.** JUnit 5 parameterized and CSV-driven tests, integration tests against an embedded **HSQLDB** instance via Spring's `EmbeddedDatabaseBuilder`, and Mockito-mocked service-layer unit tests.
- **Dependency injection** through Spring's IoC container, wiring pluggable component implementations from XML configuration.

**Module breakdown**

| Module | Focus | What was built |
|---|---|---|
| **Module05** | JDBC & relational modelling | Chat domain model, normalized schema, repository interfaces + JDBC implementations, HikariCP pooling |
| **Module06** | Testing | JUnit 5 (parameterized, `@CsvFileSource`), embedded HSQLDB integration tests, Mockito service mocks |
| **Module07** | Reflection & annotations | Reflection console tool, HTML-form annotation processor, mini-ORM (`OrmManager`) |
| **Module08** | Spring | IoC container, XML-defined dependency injection across pluggable `Printer` / `Render` / `PreProcessor` implementations |

**Schema**

```sql
Users(user_id PK, login UNIQUE, password)
Chatroom(chatroom_id PK, name, owner_id FK → Users)
Message(message_id PK, author_id FK → Users, chatroom_id FK → Chatroom, text, date_time)
User_Chatrooms(user_id FK, chatroom_id FK, PRIMARY KEY(user_id, chatroom_id))
```

</details>

<details>
<summary><b>minishell — pipeline stages and engineering challenges</b></summary>

<br>

```
input ──► LEXER ──► EXPANSION ──► PARSER ──► EXECUTION ──► output
         tokenize    $VAR, $?      command      fork/exec
         + quotes    resolution    structures   + pipes
```

**Highlights**

- **State-machine tokenizer** handling single- and double-quoting rules, operator recognition and ambiguous-redirect detection, feeding a parser that builds the command and redirection structures.
- **Pipeline execution** with `fork` / `pipe` / `dup2` process orchestration and `PATH` resolution for external commands.
- **All four redirection types** — `<`, `>`, `>>` and heredoc (`<<`) — with correct `open` flags per type and heredoc input loops.
- **Seven built-ins** (`echo`, `cd`, `pwd`, `export`, `unset`, `env`, `exit`) with Bash-accurate exit statuses and environment mutation.
- **Bash-accurate signal semantics** for `SIGINT`, `SIGQUIT` and EOF across interactive, in-pipeline and heredoc contexts.
- **Custom tracking allocator** that registers every heap allocation, enabling deterministic single-call cleanup and **zero leaks under Valgrind**.

**Engineering challenges**

- **Quoting is context-dependent.** `'$VAR'` must stay literal while `"$VAR"` expands, and expansion happens *after* tokenizing but *before* parsing — getting that ordering wrong breaks everything downstream.
- **File-descriptor hygiene.** Every `dup2` in a pipeline needs its original descriptor closed in both parent and child, or the shell hangs waiting on a pipe that never sees EOF.
- **Signals behave differently** in the interactive prompt, inside a running child, and during a heredoc read — three distinct handler configurations.
- **Memory discipline** without a garbage collector in C, solved by making one instead: a tracking allocator that owns every pointer.

</details>

<details>
<summary><b>inception — stack architecture</b></summary>

<br>

```
                        ┌─────────────┐
          port 443      │    NGINX    │   sole entrypoint
 User ────────────────► │  TLS 1.2/3  │
                        └──────┬──────┘
                 ┌─────────────┼─────────────┐
                 ▼             ▼             ▼
          ┌────────────┐ ┌──────────┐ ┌────────────┐
          │ WordPress  │ │ Adminer  │ │  Website   │
          │  PHP-FPM   │ │          │ │            │
          └─────┬──────┘ └──────────┘ └────────────┘
           ┌────┴────┐
           ▼         ▼
     ┌──────────┐ ┌───────┐        isolated bridge network
     │ MariaDB  │ │ Redis │        + FTP + local DNS
     └──────────┘ └───────┘
```

**Highlights**

- **NGINX as the sole entry point** — a TLSv1.2/1.3 reverse proxy on port 443. Every other service is unreachable from the host.
- **Network isolation.** All containers communicate over a private Docker bridge network; only NGINX publishes a port.
- **Service set:** NGINX, WordPress on PHP-FPM, MariaDB, Redis object cache, Adminer, vsftpd, a dnsmasq local resolver, and a static site.
- **Secrets and persistence.** Credentials managed through Docker secrets and environment files; data durability across container restarts guaranteed by persistent named volumes.
- **Reproducibility.** The whole stack comes up with a single `make` — no manual configuration steps.

</details>

<details>
<summary><b>cub3D and philosophers — technical detail</b></summary>

<br>

**cub3D**

- **DDA raycasting.** One ray per screen column; horizontal and vertical grid intersections are computed independently and the nearer hit wins.
- **Fisheye correction** through perpendicular-distance projection: `wall_height = TILE_SIZE / perpendicular_distance × dist_to_projection_plane`.
- **Validating scene parser** for `.cub` files — directional wall textures (NO/SO/WE/EA), floor and ceiling RGB values, map enclosure verification and player start position — rejecting malformed maps before a single frame is drawn.
- **Per-direction texture mapping** with pixel-level color extraction and texture-coordinate computation per ray.
- **Smooth input** via boolean key-state tracking, so multiple keys held at once (WASD + arrows + sprint) compose correctly, plus a toggleable minimap.

**philosophers**

- **One mutex per shared resource**, with a separate mutex guarding printed output so status lines never interleave.
- **Deadlock avoidance without a central arbiter** — parity-ordered fork acquisition plus a staggered thread start breaks the circular-wait condition.
- **Starvation detection.** A dedicated monitoring thread compares each philosopher's `last_meal` timestamp against `time_to_die` at millisecond resolution and raises a shared stop flag.
- **Clean shutdown.** All threads observe the stop flag, are joined, and every mutex is destroyed — no leaks, no orphaned threads.

</details>

---

<!-- ═══════════════════════ ADDITIONAL PROJECTS ═══════════════════════ -->

## Additional Projects

<details>
<summary><b>Systems &amp; Networking</b></summary>

<br>

**NetPractice** — *Networking, IP addressing, routing*
Diagnosed and repaired broken network topologies across a progressive series of configuration exercises: assigning valid IP addresses, subnet masks and default routes across hosts, switches and routers to restore end-to-end connectivity. Applied CIDR subnetting, address-range and network-collision analysis, and routing-table design under constrained address space.

**[minitalk](https://github.com/obbad-dev/minitalk)** — *C, UNIX signals, IPC*
A client/server messaging system built **only on `SIGUSR1` / `SIGUSR2`** — no sockets, no pipes. The client encodes each character bit by bit (MSB→LSB); the server reassembles bytes in a `SA_SIGINFO` handler and acknowledges every bit back to the sender. The per-bit ACK handshake is what makes transmission reliable despite signals having no queueing guarantees.

</details>

<details>
<summary><b>Algorithms &amp; C Fundamentals</b></summary>

<br>

**[push_swap](https://github.com/obbad-dev/pushSwap)** — *C, algorithms, data structures*
Sorts integers across two stacks using only a restricted operation set (`swap` / `push` / `rotate` / `reverse rotate`), minimizing total move count. Uses **rank compression** to map values onto `1..n`, a **chunked bucketing pass** with a sliding rank window (10 for ≤100 elements, 30 beyond), and position-aware rotation selection — choosing rotate vs. reverse-rotate by which half of the stack the target sits in. Specialized routines handle sizes 2, 3 and ≤7 directly.

**[libft](https://github.com/obbad-dev/libft)** — *C, static library*
A reimplementation of a practical subset of the C standard library, packaged as `libft.a`: memory operations (`memset`, `memcpy`, `memmove`, `memchr`, `memcmp`), string manipulation (`strlen`, `strlcpy`, `strlcat`, `strnstr`, `split`, `substr`, `strjoin`, `strtrim`), conversions (`atoi` with overflow handling, `itoa`), character classification, file-descriptor I/O, and a singly linked list API — all built on `write` and `malloc` alone.

**[ft_printf](https://github.com/obbad-dev/ft_printf)** — *C, variadic functions*
A drop-in `printf` replacement covering `%c %s %p %d %i %u %x %X %%`, writing directly through `write(2)` with no stdio buffering. Uses `va_list` for variadic dispatch and recursion for digit emission, so higher-order digits print first without intermediate buffers, while returning an accurate count of bytes written.

**[so_long](https://github.com/obbad-dev/so_long)** — *C, MiniLibX, graph traversal*
A 2D tile-based game with a full map-validation pipeline: rectangularity, character-set validation, wall enclosure, and **flood-fill reachability** proving every collectible and the exit are actually reachable from the player's start before the game launches. Rendering and event handling run through MiniLibX with per-facing player textures.

</details>

<details>
<summary><b>C++ &amp; Object-Oriented Design</b></summary>

<br>

**[CPP00 – CPP09](https://github.com/obbad-dev?tab=repositories&q=CPP)** — *C++98*
The full 42 C++ module series, working up from the basics to generic programming:

| Modules | Focus |
|---|---|
| `CPP00 – CPP02` | Classes, member functions, the orthodox canonical form, operator overloading, fixed-point arithmetic |
| `CPP03 – CPP04` | Inheritance, abstract classes, interfaces, polymorphism, deep copying |
| `CPP05 – CPP06` | Exception handling, custom exception hierarchies, the four C++ casts, type conversion |
| `CPP07 – CPP09` | Templates, template classes, STL containers, iterators and algorithms |

</details>

<details>
<summary><b>Java Fundamentals</b></summary>

<br>

**[Java_concepts](https://github.com/obbad-dev/pool_java)** — *Java (Modules 00–04)*
The first half of the Java curriculum, ahead of the backend suite above:

| Module | Focus |
|---|---|
| `Module00` | Core syntax, control flow, arrays |
| `Module01` | OOP — classes, inheritance, interfaces, packaging |
| `Module02` | Collections, generics, exceptions |
| `Module03` | **Multithreading** — `Thread` vs. `Runnable`, a ping-pong synchronization exercise, a concurrent downloader splitting tasks across threads |
| `Module04` | An **image-to-ASCII converter** with color mapping — file I/O, image decoding and a layered `app` / `logic` package structure |

</details>

<details>
<summary><b>Databases &amp; Scripting</b></summary>

<br>

**[Database workshops](https://github.com/obbad-dev/worshop_of_data_base)** — *PostgreSQL, Bash*
A mini academic system modelled across `students`, `majors`, `courses` and a `majors_courses` junction table, with schema and constraint dumps checked in alongside CSV data sources and a **Bash ETL pipeline** (`insert_data.sh`) that imports and normalizes the raw data.

**[Celestial Bodies Database](https://github.com/obbad-dev/Celestial_Bodies_Database)** — *PostgreSQL*
A relational model of the universe — `galaxy`, `star`, `planet`, `moon` — with typed columns (`numeric(8,2)` distances, boolean flags), primary keys on every table and foreign keys threading the hierarchy together.

**[basic_bash_scripting](https://github.com/obbad-dev/basic_bash_scripting)** — *Bash*
Shell scripting fundamentals: control flow, argument handling and text processing.

**[html-css](https://github.com/obbad-dev/html-css)** — *HTML, CSS*
Responsive layouts with Flexbox and Grid.

</details>

---

<!-- ═══════════════════════ LANGUAGES ═══════════════════════ -->

## Languages

| Language | Level |
|---|---|
| **English** | Intermediate |
| **French** | Intermediate |

---

<!-- ═══════════════════════ STATS ═══════════════════════ -->

## GitHub Activity

<p align="center">
  <img src="https://github-profile-summary-cards.vercel.app/api/cards/repos-per-language?username=obbad-dev&theme=github_dark" height="200" alt="Languages by repository" />
  <img src="https://github-profile-summary-cards.vercel.app/api/cards/most-commit-language?username=obbad-dev&theme=github_dark" height="200" alt="Languages by commit" />
</p>

<p align="center">
  <img src="https://github-profile-summary-cards.vercel.app/api/cards/profile-details?username=obbad-dev&theme=github_dark" width="87%" alt="Profile summary" />
</p>

<p align="center">
  <img src="https://streak-stats.demolab.com?user=obbad-dev&theme=tokyonight&hide_border=true&background=0D1117&ring=F7A41D&fire=F7A41D&currStreakLabel=F7A41D&sideLabels=c9d1d9&dates=6e7681" height="180" alt="Contribution streak" />
</p>

<picture>
  <source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/obbad-dev/obbad-dev/output/github-contribution-grid-snake-dark.svg" />
  <source media="(prefers-color-scheme: light)" srcset="https://raw.githubusercontent.com/obbad-dev/obbad-dev/output/github-contribution-grid-snake.svg" />
  <img src="https://raw.githubusercontent.com/obbad-dev/obbad-dev/output/github-contribution-grid-snake.svg" alt="Contribution snake" width="100%" />
</picture>

---

<!-- ═══════════════════════ NOW ═══════════════════════ -->

## Currently

```diff
+ Building ft_transcendence — real-time WebSocket services over a containerized stack
+ Learning Spring Boot — from the Spring core container toward REST APIs and persistence
+ Deepening PostgreSQL — query design, indexing, transaction behaviour
+ Continuing the 42 cursus at 1337 (UM6P) Rabat
! Seeking a backend engineering internship
```

<p align="center">
  <a href="mailto:oualidobbad@gmail.com"><img src="https://img.shields.io/badge/Let's%20talk-oualidobbad@gmail.com-F7A41D?style=for-the-badge&logo=minutemailer&logoColor=white" alt="Contact" /></a>
  <a href="https://www.linkedin.com/in/oualid-obbad-0b4102302/"><img src="https://img.shields.io/badge/LinkedIn-0A66C2?style=for-the-badge&logo=linkedin&logoColor=white" alt="LinkedIn" /></a>
</p>

<img src="https://capsule-render.vercel.app/api?type=waving&color=0:F7A41D,55:1f2937,100:0d1117&height=110&section=footer" width="100%" alt="" />
